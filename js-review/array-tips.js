// Create a Range of Numbers
const range = Array(100).fill(0).map((_, i) => i + 1);
const range2 = [...Array(100).keys()];

// GET A RANDOM ELEMENT
const random = arr[Math.floor(Math.random() * arr.length)]; 

// Intere with property names

// Array function
// arr.forEach();
// arr.map()
// arr.filter();
// arr.find();
// arr.findIndex();
// arr.reduce();